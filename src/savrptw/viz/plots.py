"""Paper figures — regenerated from real experiment results only.

No fabricated lines (killing the old `ga_time × 0.05` DRL point), no raw-sample
"Pareto" plots (dominance filtering from FORMULATION.md §13 is mandatory).
"""

from __future__ import annotations


def scalability_plot(*args, **kwargs):
    raise NotImplementedError("Scalability plot — scheduled for Task #14.")


def pareto_plot(*args, **kwargs):
    raise NotImplementedError("Pareto plot — scheduled for Task #14.")


def convergence_plot(*args, **kwargs):
    raise NotImplementedError("Convergence plot — scheduled for Task #14.")
